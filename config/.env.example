# Configurações do Flask
FLASK_APP=src/app.py
FLASK_ENV=development
SECRET_KEY=sua_chave_secreta_super_segura_aqui

# Configuração do Banco de Dados (SQLite para desenvolvimento/local, PostgreSQL para produção)
DATABASE_URL=sqlite:///../data/leads.db
# Para o Azure SQL Database ou PostgreSQL em produção, adicione a string de conexão real no painel de ambiente.
# DATABASE_URL=postgresql://usuario:senha@localhost/bravvi_leads
